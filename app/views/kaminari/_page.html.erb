<li>
  <% if page.current? %>
    <span class="px-3 py-2 text-white bg-blue-500 rounded-md"><%= page.number %></span>
  <% else %>
    <%= link_to page, url, remote: remote, rel: page.rel, class: "px-3 py-2 bg-gray-300 hover:bg-gray-400 rounded-md" %>
  <% end %>
</li>
